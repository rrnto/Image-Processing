# Image-Processing
In this repository, we compare two image denoising methods: Fourier transform-based filtering and wavelet denoising. The effectiveness of each method is evaluated using the Peak Signal-to-Noise Ratio (PSNR).
